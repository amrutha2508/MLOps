# getting models from model registry
1. get model from model service using the run id
2. remove dependency on tracking server - get the model directly from s3
3. make use of make_pipeline to have dictvectorizer and model in one artifact

